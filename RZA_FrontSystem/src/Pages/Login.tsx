import LoginF from "../Components/LoginForm/LoginForm";

/* The `function Login(){` is defining a functional component named `Login`. This component returns the
JSX code for rendering a login form component (`<LoginF />`). When this component is used in a React
application, it will display the login form on the screen. */

function Login(){
    return(
        <>
            <LoginF />       
        </>
    )
}

export default Login;