import SignUpF from "../Components/SignUpForm";

/* The `function Signup(){` is defining a functional component named `Signup`. This component returns
the JSX code for rendering the `SignUpF` component. When this component is used in a React
application, it will render the content of the `SignUpF` component. */

function Signup(){
    return(
        <>
            <SignUpF />        
        </>
    )
}

export default Signup;